//todoinput should do the following
//we want the user to input a todo item in the textbox
//1a. we want to validate the input: if the text;;;box is blank and the user click submit, it should throuw an alert toinput something. othrwise we shoulj append the task to the array.
// should throw an alert to imput by connecting the inpput tag with the useState
//2. we want to have a button that will perform some operation to append the todo array.
//3. we want to have a button that will perform


const TodoInput = () => {

        return (
            <>
            
            </>
        );
}

export default TodoInput;